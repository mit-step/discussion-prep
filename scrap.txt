mcp-rag-server/
  src/
    ingestion/
      convert.py        # docling: file -> DoclingDocument
      chunk.py           # HybridChunker wrapper
      embed.py           # text -> vector, batched
      index.py           # write chunks+vectors+bm25 index to store
    retrieval/
      lexical.py          # bm25s query
      vector.py           # pgvector query
      fuse.py              # RRF combination
      retrieve.py         # orchestrates the above, returns ranked chunks
    mcp/
      server.py           # MCP server entrypoint, tool registration
      tools.py             # the actual tool(s) exposed, e.g. "search_docs"
    store/
      schema.py            # DB schema / connection setup
    config.py               # embedding model name, chunk size, db url, etc.
  data/
    raw/                   # source documents, gitignored
    processed/               # intermediate chunk output for debugging
  scripts/
    reindex.py             # CLI: rebuild the whole index from raw/
  tests/
  pyproject.toml

mv ../mpc_rag_server ../mcp_rag_server
cd ../mcp_rag_server/src
mv retrival retrieval