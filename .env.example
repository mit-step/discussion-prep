# Copy this file to .env in the repo root (same level as this file) and fill in real values.
# parley.py loads .env from the repo root regardless of which script is run.

# MIT Parley gateway key (https://parley.api.mit.edu) — used by evaluator-agent/Parley/parley.py
PARLEY_API_KEY=
